---
deployment:
  tasks:
    - export DEPLOYPATH=/home/stuw8183/public_html/repositories/undangan-digital.studio-pasti.net
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH
    - php composer.phar install --no-dev --optimize-autoloader --no-scripts
    - php artisan config:cache
    - php artisan route:cache
    - php artisan view:cache